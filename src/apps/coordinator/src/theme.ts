import { createTheme } from "@mantine/core";

// Default Mantine theme for now; the coordinator app's visual identity is filled
// in as feature screens arrive. Kept as a seam so theming has one home.
export const theme = createTheme({});
