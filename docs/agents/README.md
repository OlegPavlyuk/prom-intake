# docs/agents - Skill Configuration

Machine-facing configuration that the engineering skills read at runtime. Generated by the
`/setup-matt-pocock-skills` skill; safe to edit by hand afterwards.

| File | Purpose | Consumed by |
| ---- | ------- | ----------- |
| `issue-tracker.md` | Where issues/PRDs live and the exact `gh` commands to operate them | `/to-spec`, `/to-tickets`, `/triage`, `/wayfinder` |
| `triage-labels.md` | Maps the five canonical triage roles to this repo's label strings | `/triage`, `/to-spec`, `/to-tickets` |
| `domain.md` | Rules for reading `CONTEXT.md` + `docs/adr/` before exploring | all exploring skills |

Re-run `/setup-matt-pocock-skills` only to switch issue trackers or start over; otherwise edit these
files directly.
